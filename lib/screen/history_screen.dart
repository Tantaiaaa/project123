import 'package:flutter/material.dart';

class historysreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "ตารางการเข้าเรียน",
          textScaleFactor: 1.5,
        ),
        backgroundColor: Color.fromARGB(255, 122, 191, 248),
      ),
    );
  }
}
